{
  "name": "backend",
  "version": "1.0.0",
  "description": "Express backend with layered structure",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon src/app.js",
    "lint": "eslint src scripts --ext .js",
    "lint:fix": "eslint src scripts --ext .js --fix",
    "format": "prettier src scripts --write",
    "seed:supply": "node scripts/seed-supply.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.3.1",
    "express": "^4.18.2",
    "mongoose": "^8.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
